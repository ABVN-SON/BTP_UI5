_schema-version: 3.3.0
ID: BTP_Project
description: A simple CAP project.
version: 1.0.0
modules:
- name: BTP_Project-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: BTP_Project-auth
  - name: BTP_Project-db
  - name: BTP_Project-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: BTP_Project
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: BTP_Project-auth
  - name: BTP_Project-destination
  - name: BTP_Project-html5-runtime
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: BTP_Project-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: BTP_Project-db
  parameters:
    buildpack: nodejs_buildpack
- name: BTP_Project-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
  - name: BTP_Project-html5-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: app/
    requires:
    - artifacts:
      - project1.zip
      name: BTP_Projectproject1
      target-path: app/
    - artifacts:
      - myBTProject.zip
      name: myBTProject
      target-path: app/
- name: BTP_Projectproject1
  type: html5
  path: app/project1
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: BTP_Project-destination-content
  type: com.sap.application.content
  requires:
  - name: BTP_Project-destination
    parameters:
      content-target: true
  - name: BTP_Project-html5-repo-host
    parameters:
      service-key:
        name: BTP_Project-html5-repo-host-key
  - name: BTP_Project-auth
    parameters:
      service-key:
        name: BTP_Project-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: BTP_Project_html_repo_host
          ServiceInstanceName: BTP_Project-html5-service
          ServiceKeyName: BTP_Project-html5-repo-host-key
          sap.cloud.service: BTP_Project
        - Authentication: OAuth2UserTokenExchange
          Name: BTP_Project_uaa
          ServiceInstanceName: BTP_Project-xsuaa-service
          ServiceKeyName: BTP_Project-auth-key
          sap.cloud.service: BTP_Project
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: myBTProject
  type: html5
  path: app/project1
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: BTP_Project-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        redirect-uris:
        - https://~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: BTP_Project-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-name: BTP_Project-xsuaa-service
    service-plan: application
  requires:
  - name: app-api
- name: BTP_Project-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: BTP_Project-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: BTP_Project-srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
    service: destination
    service-plan: lite
  requires:
  - name: srv-api
- name: BTP_Project-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: BTP_Project-html5-service
    service-plan: app-host
- name: BTP_Project-html5-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
